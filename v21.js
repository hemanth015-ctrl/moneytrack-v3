(()=>{const q=id=>document.getElementById(id);const home=q('home');if(!home||q('v21Gmail'))return;const p=document.createElement('section');p.id='v21Gmail';p.className='panel v21-panel';p.innerHTML='<div class="v21-head"><div><div class="label">V21 Gmail Capture</div><h3>Review Gmail expenses</h3><div class="muted" id="v21Status">Review-first mode • nothing is added without approval.</div></div><button id="v21Refresh">Refresh</button></div><div id="v21List" class="v21-list"><div class="muted">Checking for candidates…</div></div>';home.appendChild(p);const fmt=n=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(+n||0);function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function duplicate(c){return (d.tx||[]).some(t=>!t.income&&Math.abs((+t.amount||0)-(+c.amount||0))<0.01&&(t.date||'')===(c.transaction_date||'')&&((t.gmailMessageId&&t.gmailMessageId===c.gmail_message_id)||((t.note||'').toLowerCase().includes((c.merchant||'').toLowerCase())&&c.merchant)))}async function setStatus(id,status){if(!sb)return;await sb.from('gmail_expense_candidates').update({status,reviewed_at:new Date().toISOString()}).eq('id',id);await load()}window.v21Approve=async id=>{if(!sb)return toast('Cloud connection unavailable');const {data:c,error}=await sb.from('gmail_expense_candidates').select('*').eq('id',id).single();if(error||!c)return toast('Could not load candidate');if(duplicate(c)){await setStatus(id,'duplicate');return toast('Duplicate detected')};MoneyTrackCore?.snapshot?.('before Gmail expense approval');d.tx.push({id:Date.now()+Math.random(),amount:+c.amount||0,date:c.transaction_date||today(),cat:c.category||smartCategory(c.merchant||c.subject||'','Other'),note:c.merchant||c.subject||'Gmail expense',income:c.direction==='income',gmailMessageId:c.gmail_message_id,cardLast4:c.card_last4||null,source:'gmail-v21'});save();await setStatus(id,'approved');render();toast('Gmail expense added')};window.v21Reject=id=>setStatus(id,'rejected');async function load(){const list=q('v21List'),status=q('v21Status');if(!sb){status.textContent='Supabase client unavailable';list.innerHTML='<div class="muted">Cloud connection is required for Gmail review.</div>';return}const {data:{user}}=await sb.auth.getUser();if(!user){status.textContent='Sign in to Cloud Sync to review Gmail expenses.';list.innerHTML='<div class="muted">Use the existing Cloud Sync sign-in first.</div>';return}const {data,error}=await sb.from('gmail_expense_candidates').select('*').eq('status','pending').order('created_at',{ascending:false}).limit(50);if(error){status.textContent='Gmail backend is not ready yet.';list.innerHTML='<div class="muted">Review queue schema is waiting for backend activation.</div>';return}status.textContent=`${data.length} pending • review-first mode`;list.innerHTML=data.length?data.map(c=>`<article class="v21-item"><div><strong>${esc(c.merchant||'Unknown merchant')}</strong><div class="muted">${esc(c.transaction_date||'')} • ${esc(c.payment_method||'')} ${c.card_last4?'••'+esc(c.card_last4):''}</div><div class="muted">${esc(c.subject||'')}</div></div><div class="v21-actions"><strong>${fmt(c.amount)}</strong><button onclick="v21Approve('${c.id}')">Approve</button><button class="ghost" onclick="v21Reject('${c.id}')">Reject</button></div></article>`).join(''):'<div class="muted">No Gmail expenses waiting for review.</div>'}q('v21Refresh').onclick=load;load();})();